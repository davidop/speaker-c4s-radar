---
phase: "13"
plan: "13-02"
type: feature
wave: 1
depends_on: ["13-01"]
files_modified:
  - .github/workflows/ci.yml
  - .github/dependabot.yml
autonomous: true
requirements: [QUAL-02]
---

<objective>
Implementar quality gates visibles en PR y mantenimiento automatico de dependencias para reforzar confianza tecnica del repositorio.
</objective>

<tasks>
- Crear workflow `ci.yml` para PRs y pushes relevantes con `npm test` y `npm run build` en `demo/`.
- Definir filtros de paths para evitar ejecuciones innecesarias fuera de `demo/**` y workflows.
- Configurar `.github/dependabot.yml` para npm (`demo`) y GitHub Actions.
- Verificar que la configuracion no rompa el workflow actual de GitHub Pages.
</tasks>

<verification>
1. Un cambio en `demo/**` dispara CI y ejecuta test + build.
2. CI falla correctamente ante errores en test/build.
3. Dependabot queda configurado para abrir PRs periodicos.
</verification>

<success_criteria>
- [ ] CI workflow para PRs operativo
- [ ] Build + test obligatorios en CI
- [ ] Dependabot configurado para npm y GitHub Actions
</success_criteria>
