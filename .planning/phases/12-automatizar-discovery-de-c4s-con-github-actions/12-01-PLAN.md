---
phase: "12"
plan: "12-01"
type: feature
wave: 1
depends_on: []
files_modified:
  - .github/workflows/c4s-discovery.yml
  - demo/scripts/discover-c4s.mjs
  - demo/scripts/c4s-sources.json
  - demo/scripts/c4s-sources.example.json
  - demo/package.json
  - README.md
autonomous: true
requirements: [AUTO-01, AUTO-02]
---

<objective>
Implementar un pipeline automatizado y seguro de discovery C4S que proponga cambios por PR sin romper el flujo de demo.
</objective>

<tasks>
- Implementar script `discover-c4s.mjs` con carga de fuentes, normalizacion y dedupe.
- Generar reportes de ejecucion (`discovery-report.json`, `discovery-summary.md`).
- Configurar fuentes por defecto vacias y plantilla de ejemplo para opt-in.
- Crear workflow `c4s-discovery.yml` (cron + manual) con quality gates y creacion de PR.
- Documentar setup de automation en README y script npm.
</tasks>

<verification>
1. `cd demo && npm run discover:c4s` ejecuta correctamente.
2. Con fuentes vacias por defecto, no hay cambios involuntarios en `calls.json`.
3. `cd demo && npm test` pasa.
4. `cd demo && npm run build` pasa.
</verification>

<success_criteria>
- [ ] Discovery script funcional y configurable
- [ ] Workflow GitHub Actions creado
- [ ] PR automatica con resumen configurada
- [ ] Validaciones locales (discover, test, build) en verde
</success_criteria>
